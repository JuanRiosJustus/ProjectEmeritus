package main.constants;

public class PointUtils {
//    public static void lerp(Point start, Vector3f end, float percent, Vector3f result) {
//        float x = start.x + percent * (end.x - start.x);
//        float y = start.y + percent * (end.y - start.y);
//        result.x = x;
//        result.y = y;
//    }
}
